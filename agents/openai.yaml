interface:
  display_name: "Playwright MCP 路由技能"
  short_description: "统一使用 Playwright MCP 工具执行网页自动化流程"
  default_prompt: "使用 $plyawright-skill 处理当前网页自动化任务，并严格只调用 mcp__Playwright__* 前缀工具；默认先通过 PowerShell 脚本 C:\\Users\\shang\\.codex\\skills\\plyawright-skill\\assets\\start-local-chrome-9224.ps1 启动或确认本机 Chrome 调试实例，再连接 127.0.0.1:9224，复用 D:\\chrome-mcp-pw 持久化目录对应的同一浏览器实例，除非用户明确要求扩展模式，否则不要偏离默认流程。"

policy:
  allow_implicit_invocation: true
